[build]
  # ...

[functions]
  directory = "netlify/functions"
  included_files = ["backend/**"]
  
  # ***** التعديل الحاسم هنا *****
  # نحدد مجلد نشر غير موجود عمداً، أو مجرد نقطة (لأننا ننشر دوال فقط).
  # سنستخدم نقطة للتأكد من تجاوز المشكلة.
  publish = "." 

[functions]
  # هذا السطر يطلب من Netlify تضمين مجلد 'backend' بالكامل،
  # حيث توجد ملفات 'services' و 'server.py'
  included_files = ["backend/**"]
  
[[redirects]]
  from = "/api/*" 
  to = "/.netlify/functions/main" 
  status = 200